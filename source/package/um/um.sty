% main.tex からの相対パス
\NewDocumentCommand{\um@ルートフォルダ}{}{../../../..}
\NewDocumentCommand{\um@ソースフォルダ}{}{../../..}
\NewDocumentCommand{\um@コモンフォルダ}{}{\um@ソースフォルダ/common}
\NewDocumentCommand{\コモンフォルダ}{}{\um@コモンフォルダ}
\NewDocumentCommand{\um@パッケージフォルダ}{}{\um@ソースフォルダ/package}


% パッケージオプション定義
\DeclareKeys[um@config]{
  スタイル.store = \um@スタイル,
  数式スタイル.store = \um@数式スタイル,
  % 索引を出力.if = 索引を出力,
}

% パッケージオプションのデフォルト値
\SetKeys[um@config]{
  スタイル = draft,
  数式スタイル = {math-style=literal, normal-style=ISO},
}

% パッケージオプション読み込み
\ProcessKeyOptions[um@config]


% スタイル読み込み
\usepackage{\um@パッケージフォルダ/um/um_\um@スタイル}

% マクロ読み込み
\usepackage{\um@パッケージフォルダ/um_macro/um_macro}

% パッチ読み込み
\usepackage{../_package_local/um_patch}
